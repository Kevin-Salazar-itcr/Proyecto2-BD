@model IEnumerable<ProyectoCRM.Models.VistasCotTipo>

@{
    ViewData["Title"] = "Index";
}

<h1>cotizaciones por tipo</h1>

<div id="chart_div">
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <script>
                elements.push(["@item.Tipo", @item.Total]);
            </script>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>

        </tr>
}
    </tbody>
</table>

@*se indican el titulo, las dimensiones y las columnas que tendrá el grafico*@
<script>
    title = 'Cotizaciones por tipo';
    width = 600;
    height = 600;
    columns.push(["string", "Tipo"]);
    columns.push(["number", "Total"]);
    google.charts.setOnLoadCallback(pieChart); // circular
    //google.charts.setOnLoadCallback(BarChart); // barra
</script>
